version: "3.9"

services:
  web:
    build:
      context: ./backend
    container_name: fastapi_clean_app
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
